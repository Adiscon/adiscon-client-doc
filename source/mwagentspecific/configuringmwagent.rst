.. _mwagent-configuration:

Configuration
=============

Use this section to configure how MonitorWare Agent collects data, evaluates
rules, and stores or forwards the result.

Configuration is created in the **MonitorWare Agent Configuration Client** and
then applied to the **MonitorWare Agent Service**. Save and apply changes after
editing so the running service uses the updated configuration.

In this manual, **input** is the clearest plain-language concept for anything
that collects or receives data, while **service** remains the operational term
for the configured MonitorWare Agent object.

.. toctree::
   :maxdepth: 1

   clientoptions
   clienttools
   usingfilebasedconfiguration
   generaloptions
   services
   filterconditions
   actions
   ../monitorwareconcepts
